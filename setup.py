from setuptools import setup

setup(name='Demo',
      version='0.1',
      description='super simple module',
      author='hallie parten',
      author_email='hallie parten',
      license='APACHE',
      packages=['demo'])
